# Object类

所有的类，都直接或者间接的继承了Object类。Object类的方法是一切子类都可以直接使用的。

## Object类的常用方法：toString()

| 方法名                   | 说明                                                         |
| ------------------------ | ------------------------------------------------------------ |
| public String toString() | 默认是返回当前对象在堆内存中的地址信息:类的全类名@十六进制哈希值 |

### 📖知识点讲解

1. **默认行为**`toString()`是Object里面定义的方法。如果我们自己写的类**没有重写toString()**，调用这个方法的时候，就执行Object里面的原版实现。输出格式：`类的全类名@十六进制哈希值`

   > 💡注意：这里打印出来的并不是对象真正的内存物理地址，是hashCode之后转为十六进制的值！（很多初学者会误以为这就是内存地址）

2. **开发里面的问题（两条要点）**

- 开发中直接输出对象，默认输出对象的地址其实是毫无意义的。
- 开发中输出对象变量，更多的时候是希望看到对象的内容数据而不是对象的地址信息。

👉 **解决方案：重写toString()方法！**我们在自己写的实体类当中重写`toString()`，让这个方法返回对象里面各个成员变量的数据，以后直接打印对象的时候，就会自动调用重写后的toString，直接输出对象里面的数据，不再打印`xxx@123abc`这种哈希字符串。

### ✨代码小示例（便于理解）

```
class Student{
    private String name;
    private int age;
    public Student(String name, int age) {
        this.name = name;
        this.age = age;
    }
    //没有重写toString的时候
}

public class Test {
    public static void main(String[] args) {
        Student s = new Student("张三",18);
        //直接打印对象，底层会自动调用s.toString()
        System.out.println(s);
        //输出类似：Student@7852e922 （全类名@十六进制哈希值）
    }
}
```

重写toString之后：

```
@Override
public String toString() {
    return "Student{" +
            "name='" + name + '\'' +
            ", age=" + age +
            '}';
}
//打印对象输出：Student{name='张三', age=18}，打印对象内部的数据，这就是开发想要的效果
```

### 📌总结笔记

1. Object是所有类的根父类；
2. 默认`toString()`返回`全类名@十六进制哈希值`；
3. **打印对象的时候，println会自动调用对象的toString()方法；**
4. 实际开发，实体类一定要重写toString，用于查看对象内部存储的数据。

> 补充对比你两张截图：
>
> - 第一张课件写：`类的全限名@内存地址`（通俗简易说法）
> - 第二张课件写：`类的全类名@十六进制哈希值`（严谨说法！**它并不是真正的内存地址！只是哈希码转成十六进制**，考试建议以第二版为准）。

如果你需要，我可以继续整理Object类的equals笔记。