# 多态

同一个行为具有多个不同表现形式或形态的能力 

多态的3个前提（必背）

1. 存在继承关系
2. 子类重写父类的成员方法
3. 父类引用指向子类对象（向上转型）核心写法：Fu f = new Zi();

示例代码

```
//父类
class Fu {
    int num = 10;
    public void show() {
        System.out.println("父类方法");
    }
}

//子类
class Zi extends Fu {
    int num = 20;
    @Override
    public void show() {
        System.out.println("子类重写的方法");
    }
}

public class Test {
    public static void main(String[] args) {
        //方式1：普通创建子类对象，没有多态
        Zi z = new Zi();
        System.out.println(z.num); //20
        z.show(); //子类重写的方法

        //方式2：多态写法！父类引用指向子类对象
        Fu f = new Zi();
        System.out.println(f.num); //10
        f.show(); //子类重写的方法
    }
}
```

运行结果

```
20
子类重写的方法
10
子类重写的方法
```

核心口诀【考试默写】成员变量：编译看左边，运行看左边（变量没有多态！）成员方法：编译看左边，运行看右边（方法才有多态！）

通俗解释 Fu f = new Zi();f.num 输出 10左边引用类型是父类 Fu。访问成员变量的时候，直接按照引用类型取值。虽然堆里面子类对象有 num=20，但是不会读取它。成员变量没有重写概念。

f.show () 执行子类的 show ()编译阶段先检查左边父类 Fu，确认 Fu 有 show 方法；运行的时候 JVM 去看堆里面真实的对象是 Zi，就执行子类重写之后的 show 方法。这就是多态（动态绑定）。

补充知识点向下转型：Zi z2 = (Zi) f;，目的调用子类独有的方法，不是多态前提static 静态方法不存在重写，没有多态效果



多态的好处：**提高了程序的扩展性****对象多态**：将方法的形参定义为父类类型，这个方法可以接收该父类的任意子类对象**行为多态**：同一个行为，具有多个不同表现形式或形态的能力

------

#### 1、对象多态（侧重**参数、引用**层面）

> `public void useAnimal(Animal a)`形参写父类 Animal。调用的时候，既可以传`new Dog()`，也可以传`new Cat()`，以后新增`new Rabbit()`兔子对象也直接传进去。

- 不需要再写新的方法；
- 实参是子类，赋值给父类形参，发生**隐式向上转型**。👉对象多态讲的就是：**父类的引用可以接收任意子类的实体对象**，一般用在方法的形参上，这就是用来提升扩展性。

#### 2、行为多态（侧重**方法执行**层面）

> 同一个行为`eat()`吃东西。

- Dog 对象执行 eat：狗吃骨头
- Cat 对象执行 eat：猫吃鱼

**同样一个 eat 动作（行为），不同子类对象运行，做出来不一样的事情！**这就是行为多态，依靠**子类重写父类方法**实现。



> **多态弊端**：父类引用无法访问子类特有成员，**用向下转型解决**
>
> 1. **向上转型（子→父，自动）**：`Fu f = new Zi();` 父类引用指向子类对象（多态写法）
>
> 2. **向下转型（父→子，强制）**：`Zi z = (Zi)f;` 转回子类引用，就可以调用子类特有成员
>
> 3. 向下转型风险
>
>    ：实际对象和目标类型不一致，运行时报
>
>    ClassCastException类型转换异常
>
>    > 预防：使用 `instanceof` 判断之后再转型