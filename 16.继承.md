# 继承

## 继承是什么？

继承是**面向对象三大特征之一**，可以让类与类之间产生关系，也就是父类与子类（子父）的关系。

> 子类可以直接使用父类中**非私有**的成员（成员变量、成员方法）。⚠️父类当中被`private`修饰的私有成员，子类无法直接访问。

## 实现继承的关键字

- `extends`
- 继承书写格式：

```
public class 子类名 extends 父类名{

}
```

## 使用继承的好处

1. **提高代码的复用性**：多个子类重复的代码抽取到父类，子类直接复用，不用重复书写。
2. **提高代码的维护性**：以后需要修改公共逻辑时，只修改父类即可，不需要修改每一个子类。
3. **为后面多态打下基础**（继承是实现多态的前提）。

##  什么时候使用继承？

> 判断原则：类与类之间，存在相同（共性）的内容，并且满足 **is‑a（是一种）** 的关系，就可以考虑使用继承，用来优化代码。

✅举例：

- `Dog` 是一种 `Animal` → Dog继承Animal（合理，is‑a成立）❌错误示例：
- Student 和 Desk（学生和课桌），不存在is‑a关系，**不要使用继承**。

> 💡注意：不要仅仅为了复用代码就强行写继承，优先判断is‑a语义关系，否则会造成类关系混乱。

## 继承当中成员访问特点

### 5.1 成员变量

- **就近原则**：当父子类出现重名的成员变量，在子类方法中直接写变量名，优先使用**子类的成员变量**。
- 如果想要访问父类当中重名的成员变量，需要使用`super`关键字。
- 关键字辨析
  - `this`：代表本类对象，调用**本类**的成员
  - `super`：代表父类对象引用，调用**父类**的成员

示例代码

```
public class Fu {
    int num = 10;
}

class Zi extends Fu {
    int num = 20;
    public void method(){
        int num = 30;
        System.out.println(num);        //30 局部变量
        System.out.println(this.num);   //20 子类成员变量
        System.out.println(super.num);  //10 父类成员变量
    }
}
```

### 5.2 成员方法（方法重写 / 方法覆盖）

- 在继承体系中，子类可以继承父类的方法。
- 有时候子类不想原封不动使用父类的方法，希望修改方法逻辑，这时就需要**方法重写（也叫方法覆盖）**。

#### ✅重写基础要求

> 子类重写父类方法，**需要保证方法声明完全一致（方法名，参数，返回值类型需要保持一致）**

#### ⚠️重写注意事项

1. 父类当中`private`修饰的私有方法**不能够被重写**。
2. 子类重写父类方法时，**访问权限必须大于或者等于父类该方法的权限**。

权限从小到大顺序：`private` < 默认(default) < `protected` < `public`

| 权限修饰符                      | 同一个类中 | 同一个包中 | 不同包的子类 | 不同包的无关类 |
| ------------------------------- | ---------- | ---------- | ------------ | -------------- |
| **private（私有）**             | ✅可以访问  | ❌不可以    | ❌不可以      | ❌不可以        |
| **default（默认，不写修饰符）** | ✅可以访问  | ✅可以访问  | ❌不可以      | ❌不可以        |
| **protected（受保护）**         | ✅可以访问  | ✅可以访问  | ✅可以访问    | ❌不可以        |
| **public（公共）**              | ✅可以访问  | ✅可以访问  | ✅可以访问    | ✅可以访问      |



Java 只支持单继承，不支持类的多继承，但支持多层继承



## 继承‑构造方法

问题 1：子类是否可以继承到父类的构造方法？

**答案：不可以，子类需要手动编写构造方法**

> 💡重点：**构造方法不能被继承！**子类不会继承父类的构造器，但是子类的构造器**会调用父类的构造方法**，先完成父类部分的初始化，再初始化子类自己的成员。

问题 2：在子类所有的构造方法中，都默认隐藏了一句什么代码？

**答案：super();**`super();`含义：调用**父类的无参构造方法**。

### 📌几条核心规则（考试高频）

1. `super();`是子类构造方法第一行**默认隐藏的代码**，不写也存在；

2. 如果父类

   没有无参构造方法

   （父类只写了有参构造），此时子类默认的

   ```
   super()
   ```

   就会报错！

   - 这时必须**手动写 super (实参)**，去调用父类的有参构造；

3. `super(xxx);`必须写在子类构造方法里面的**第一行**，不能放在后面；

4. `this()`（调用本类其他构造）和`super()`不能同时出现，因为都要求写在第一行！

### 执行顺序

> 创建子类对象的时候：**先执行父类构造方法 → 再执行子类构造方法**先初始化从父类继承过来的那部分属性，再初始化子类新增属性。