# this 与 super

## 基础概念

- this：代表本类对象的引用
- super：代表父类存储空间的标识（不是父类对象）

## 使用对比表格

| 关键字 | 访问成员变量        | 访问成员方法          | 访问构造方法                         |
| ------ | ------------------- | --------------------- | ------------------------------------ |
| this   | this.本类成员变量;  | this.本类成员方法();  | this(参数); 调用本类其他重载构造方法 |
| super  | super.父类成员变量; | super.父类成员方法(); | super(参数); 调用父类构造方法        |

## this.成员访问规则

当局部变量和本类成员变量名字不重名时，this.可以省略。当局部变量与成员变量重名时，必须写this.，用于访问本类的成员变量。

## this调用本类重载构造方法

作用：复用本类构造方法的初始化代码，减少代码冗余。必须写在构造方法第一行，不可以省略。只能在构造方法中使用，普通方法不能使用。

```
public class Student {
    String name;
    int age;

    public Student(){
        this("张三",18);
    }

    public Student(String name,int age){
        this.name = name;
        this.age = age;
    }
}
```

## super.成员访问规则

子类与父类成员名称不重名时，super.可以省略。父子类成员重名，想要访问父类成员时，super.不可以省略，遵循就近原则。

## super调用父类构造方法

作用：初始化父类继承过来的成员数据。父类存在无参构造，子类构造默认隐藏 super()，无需手动书写。父类只有有参构造、没有无参构造时，子类必须手动书写 super(实参)，且必须在第一行。

## 互斥规则

this() 和 super() 不能同时出现在同一个构造方法中。两者都要求写在构造方法第一行，只能二选一。

## 省略规则总结

this.：局部变量与成员变量重名不可省略，不重名可省略。this()：调用构造方法，永远不能省略。super.：父子成员重名访问父类不可省略，不重名可省略。super()：父类有无参构造自动省略，无无参构造必须手动书写不可省略。

