# final

## 一、final基础概念

`final`翻译：**最终的、不可改变的**final可以用来修饰三样东西：**类、方法、变量**

### 1. final修饰类

> final修饰的类叫**最终类**特点：**这个类不能被继承，没有子类**

```
final class Animal{
}
//class Dog extends Animal{} //编译报错！无法继承final类
```

> 常见JDK里面的final类：`String`、`Integer`，这些类都不能写子类。

### 2. final修饰方法

> final修饰的方法叫**最终方法**特点：**该方法可以被子类继承使用，但是子类不能对这个方法进行重写！**⚠️注意：不是不能调用！只是禁止重写覆盖！

```
class Father{
    public final void show(){
        System.out.println("父类show方法");
    }
}
class Son extends Father{
    // public void show(){} //编译报错！不能重写final方法
}
```

### 3. final修饰变量 → 变成**常量**

> 规则：只能赋值一次，赋值完成之后，不允许再次赋值！

#### ✨常量命名规范（考试常考）

1. 普通变量：小驼峰命名 `max`，`maxValue`
2. final修饰之后的常量：**全部大写；多个单词中间使用下划线隔开** `MAX`，`MAX_VALUE`

#### 📌final修饰变量分两种情况：基本类型 & 引用类型

1）**final修饰基本类型变量（int double char等）**保存的就是数值；final之后，**里面的数据值不能修改**

```
final int NUM = 100;
// NUM = 200; //报错，不允许重新赋值
```

2）**final修饰引用类型变量（对象、数组）**

> 引用变量存的是对象在堆里面的地址！final限制的是：**保存的对象地址不能修改，不允许让这个引用再指向另一个新对象；但是对象内部的成员属性完全可以修改！**

```
final Student s = new Student();
s.setName("张三"); //✅合法，可以修改对象里面的属性
//s = new Student(); //❌报错！不允许s指向另外一个新对象（地址不能改）
```

#### 📍final修饰【成员变量】（重点简答题）

成员变量有两种赋值时机，**二选一，必须选其中一个！不能留默认值！**

1. 在定义变量的时候直接赋值

```
class Student{
    final int NUM = 60; //定义处直接赋值
}
```

1. 在构造方法里面进行赋值（所有构造方法都必须给这个final成员赋值）

```
class Student{
    final int NUM;
    public Student(){
        NUM = 60; //构造器里面赋值
    }
}
```

> ❗错误写法：既不在定义处赋值，也不在构造方法赋值，final成员变量会报错，Java不允许使用final成员变量的默认初始值！

#### 📍final修饰局部变量

局部变量写在方法里面；只需要保证**仅仅赋值一次即可**。可以声明的时候立刻赋值；也可以先声明，后面代码里赋值一次，之后不能改。

```
public void test(){
    final int a;
    a = 20; //先声明，后面赋值一次没问题
    //a = 30; //第二次赋值报错
}
```

## 📖课后问答（背诵版）

1. **final可以修饰什么？**可以修饰：方法，类，变量
2. **修饰之后各自特点？**

- 修饰方法：该方法不能被子类重写；子类依然可以调用
- 修饰类：该类不能被继承，没有子类
- 修饰变量：该变量成为常量，只允许赋值一次，不能再次赋值

1. **final修饰基本类型变量和引用类型变量有什么不同？**

- 基本类型：变量保存的数据值不能修改；
- 引用类型：变量保存的对象地址值不能修改；对象内部的属性值可以修改。

1. **final修饰成员变量，什么时候赋值？**赋值时机二选一：①定义的时候直接赋值；②在构造方法里面完成赋值。

## 📋对比表格总结

| 修饰对象              | 效果                                                  |
| --------------------- | ----------------------------------------------------- |
| final修饰类           | 类不能被继承                                          |
| final修饰方法         | 子类不能重写该方法，但可以继承调用                    |
| final修饰基本类型变量 | 存储的数据值不允许二次修改                            |
| final修饰引用类型变量 | 引用保存的对象地址不允许修改；对象里面属性值可以改    |
| final修饰成员变量     | 赋值时机：定义时赋值 / 构造方法内赋值，不能使用默认值 |

### 🧠易混淆考点辨析

1. final方法：禁止重写，**可以继承调用**；
2. final类：禁止继承，自然也就不存在子类重写；
3. final引用对象！**地址不可改 ≠ 对象里面的数据不可改（超级高频选择题坑点）**。

如果你需要，我可以出几道final的练习题。