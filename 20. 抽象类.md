# 抽象类

## 概念

**抽象方法**：把子类共性行为抽取到父类，父类无法确定该方法的具体实现逻辑，就定义成抽象方法。

> 特点：`abstract`修饰，**没有方法体，以分号结束**

**抽象类**：包含抽象方法的类必须是抽象类，类上加`abstract`；抽象类是专门用来继承的父类，**不能new创建对象**。

## 继承规则

子类继承抽象类二选一：

1. **重写实现父类全部抽象方法（常用）**
2. 子类也声明为抽象类，不用实现

> 补充：抽象类可以有普通方法、构造方法。

## 完整示例代码

```
package com.itheima.mabstract;

public class AbstractDemo1 {
    public static void main(String[] args) {

    }
}

//抽象父类
abstract class Animal{
    //抽象方法，没有方法体
    public abstract void eat();
}

//子类重写抽象方法
class Cat extends Animal{
    @Override
    public void eat() {
        System.out.println("猫吃鱼");
    }
}

class Dog extends Animal{
    @Override
    public void eat() {
        System.out.println("狗吃肉");
    }
}
```

## 简答速记

1. 有抽象方法 → 类一定是抽象类 ✔
2. 抽象类不一定含有抽象方法 ✔
3. **抽象类不能实例化(new)**
4. 子类继承抽象类：要么全部实现抽象方法，要么子类也是抽象类

## 表格速记

| 名称     | 语法要点                         |
| -------- | -------------------------------- |
| 抽象方法 | `abstract` 返回值 方法名(); 无{} |
| 抽象类   | `abstract class 类名`，不可new   |